// Define a TypeScript class to represent login credentials
export class Login {
    // Property to store the employee's unique numeric ID
    employeeId: number = 0;

    // Property to store the corporate ID as a string
    corpId: string = '';
}
